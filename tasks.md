# Task Checklist — Magic Clipper for Google Drive (Thunderbird MV3)

- [x] Initialiser les 5 fichiers de gouvernance (`README.md`, `ARCHITECTURE.md`, `CHANGELOG.md`, `AGENTS.md`, `.gitignore`)
- [x] Configurer `manifest.json` (MV3, TB 128+ ESR, permissions ATN)
- [x] Configurer le script de build `./build.sh` (livrable `.xpi` dans `dist/`)
- [x] Créer la structure des répertoires (`_locales/`, `icons/`, `lib/`, `src/`)
- [x] Porter et adapter les fichiers de traductions i18n (`_locales/en/messages.json`, `_locales/fr/messages.json`)
- [x] Implémenter le module d'authentification OAuth2 Google et le client Drive v3 (`src/shared/drive_client.js`)
- [x] Implémenter l'Event Page background MV3 (`src/background/background.js`)
- [x] Implémenter la Popup UI (Glassmorphism, sélection des pièces jointes, note/intention) (`src/popup/`)
- [x] Implémenter les générateurs client-side (PDF via jsPDF, Markdown via Turndown) (`src/content/`)
- [x] Exécuter les tests de validation manuelle et automatique (`web-ext lint`)
- [ ] Lancer le rituel de fin de sprint (`sprint-ritual`)
